Utility functions for working with the color names and color value
formats defined by the HTML and CSS specifications for use in
documents on the Web.

Support is included for the following formats (RGB colorspace only;
conversion to/from HSL can be handled by the ``colorsys`` module in
the Python standard library):

* Six-digit hexadecimal

* Three-digit hexadecimal

* Integer rgb() triplet

* Percentage rgb() triplet

And for the defined color names of the following specifications:

* HTML 4

* CSS 2

* CSS 2.1

* SVG/CSS 3

Full documentation is `available online
<http://docs.b-list.org/webcolors/1.3.1/`_.
